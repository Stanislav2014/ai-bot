# Промты — Sprint 1 (2026-04-15 … 2026-04-17)

Хронологический архив инструкций от пользователя (Stan, @StasMura) к AI-агенту (Claude Code) в течение спринта. Короткие подтверждения (`да`, `A`, `приступай`) сгруппированы с контекстом предыдущего вопроса.

Пред-спринтовые промты создания MVP — в [prompts.md](prompts.md).

---

## День 1 — 2026-04-15 (вечер до полуночи)

### Промт 1 — Развернуть doc-структуру

> подготовь проект по данной инструкции Исходный промт (2026-04-14)
>
> Первое сообщение сессии от юзера (сохранено дословно):
> 0. Установи скилл отсюда https://github.com/forrestchang/andrej-karpathy-skills
> 0.1 Посмотри структуру _docs и _board. Посмотри как ведуться задачи и какие процессы по ведению документации и прогрессу используются в проекте https://github.com/larchanka/manbot/tree/main
> 1. Создай нужную структура и заполни файлы данными (docs/tech-stack.md, architecture.md, db-schema.md, links.md, ui-kit.md, current-sprint.md)
> 2. Создай файлы docs/context-dump.md, change-request.md, legacy-warning.md
>
> [далее расширенный промт с manbot-style префиксами A/B/C/D, branch naming, TDD mandatory, karpathy guidelines, разделением change-request.md и change-request-doc.md, split по доменам (db-schema/, contracts/, setup/, workflows/), DocHub integration, и т.д.]

**Результат**: manbot-inspired структура `docs/` + karpathy-guidelines skill в `.claude/skills/`. 22 файла, ~3200 строк документации.

---

### Промт 2 — Первая задача D-04

> первая задача в спринте 1. История сообщений (псевдо-память)
> Реализуйте хранение истории диалога между пользователем и ботом.
> храните сообщения в формате: [{ "role": "user", "content": "..." }, { "role": "assistant", "content": "..." }]
> при каждом новом запросе: отправляйте в модель всю историю + новое сообщение
> опционально: использовать .yaml-формат
> 👉 Важно: история должна храниться отдельно для каждого пользователя.

**Результат**: D-04 · HistoryStore с YAML персистенцией + `/reset`.

---

### Промт 3 — Уточнение про system prompt

> System prompt: не хранится в файле, всегда prepend-ится при запросе сейчас нет системного промта?

### Промт 4 — Выбор варианта B (persistent YAML)

> B

_(Контекст: агент в brainstorming D-04 предложил варианты storage-backend для истории диалога:_
_A — in-memory dict (теряется при рестарте),_
_B — persistent YAML per-user файл (выбран),_
_C — SQLite/DB._
_См. [D-04 spec](../tasks/D-04_DIALOG_HISTORY_YAML.md))_

### Промт 5 — Доска не обновлена

> в tasks и change-request current-sprint пусто

### Промт 6 — Единая точка истины в tasks/

> 1

_(выбор между «всё в docs/tasks/» vs «spec+snapshot отдельно»)_

### Промт 7 — Приступай к TDD

> приступай

_(Контекст: после утверждения дизайна D-04 и создания task-спеки — разрешение стартовать Phase 1 (RED-GREEN-REFACTOR цикл на `HistoryStore`).)_

### Промт 8 — Разборка tangled commits (вариант A)

> А

_(вариант выноса C-01 миграции отдельным коммитом на master и пересборки D-04 ветки)_

### Промт 9 — Feedback про change-request workflow (важное правило!)

> change-request не нужно очищать, он дожен отражать те задачи которые есть в спринте

**Сохранено в memory**: `feedback_change_request.md` — change-request.md это зеркало спринта, не single-task tracker.

---

## День 2 — 2026-04-15 (продолжение)

### Промт 10 — Вторая задача D-05

> следующая задача в текущем спринте 2. Ограничение контекста
> Добавьте ограничение на размер истории:
> Возможные варианты:
> - ограничение по количеству сообщений (например, последние 10–20)
> - или по длине текста
> При превышении лимита: удаляйте самые старые сообщения

### Промт 11 — Вариант B (char limit как второй safeguard)

> B

_(Контекст: в D-05 агент предложил два подхода к ограничению контекста:_
_A — заменить существующий count-limit из D-04 (`HISTORY_MAX_MESSAGES`) на char-limit,_
_B — добавить char-limit как второй safeguard поверх count-limit (оба работают одновременно) — выбран._
_См. [D-05 spec](../tasks/D-05_CONTEXT_CHAR_LIMIT.md))_

---

### Промт 12 — Merge + D-07 System prompt

> смержи все в мастер и выполни следующую задачу. 4. System prompt
> Добавьте system prompt, который задаёт поведение модели.
> Пример: Ты опытный программист и отвечаешь кратко и по делу.
> Требуется: сделать system prompt частью контекста, убедиться, что он влияет на ответы модели

### Промт 13 — Вариант A (env-configurable)

> да

_(Контекст: в D-07 агент предложил три варианта реализации configurable system prompt:_
_A — env-переменная `SYSTEM_PROMPT` с дефолтом «Ты опытный программист…» (выбран),_
_B — per-user override через команду `/prompt`,_
_C — persona packs / presets (готовый набор с переключением)._
_См. [D-07 spec → Out of scope](../tasks/D-07_SYSTEM_PROMPT.md))_

---

### Промт 14 — Задача D-08 Логирование контекста

> следующая задача 5. Логирование контекста (обязательно)
> Перед отправкой запроса в модель:
> - логируйте полный контекст (в консоль или файл)
> - логируйте размер контекста (количество токенов)
> Задача: видеть, что именно получает модель на вход

### Промт 15 — Подтверждение дизайна

> да

_(Контекст: подтверждение дизайна D-08 — логирование на уровне `LLMClient.chat()` (покрывает и основной диалог, и summarizer), с env-гейтом `LOG_CONTEXT_FULL` для полного payload + `total_chars` + `estimated_tokens` (heuristic chars//4). См. [D-08 spec](../tasks/D-08_CONTEXT_LOGGING.md))_

---

### Промт 16 — Прогон тестов + push

> прогони тесты, запушь изменения

### Промт 17 — Проверка статуса спринта

> по текущему спринту все задачи решены?

---

### Промт 18 — Acceptance criteria (deliverable)

> Проверка результата
> Ваш бот должен: Учитывать предыдущие сообщения в диалоге; Не "забывать" контекст при коротких диалогах; Не ломаться при длинных диалогах; Показывать разницу в ответах при наличии и отсутствии контекста
> 📄 Что нужно получить в итоге
> - Ссылку на репозиторий
> - Примеры диалогов: без контекста / с контекстом
> - Краткое описание: как реализовано хранение истории, как работает суммаризация, какие проблемы возникли

**Результат**: [sprint-1-delivery.md](../sprints/sprint-1-delivery.md).

---

## День 3 — 2026-04-16

### Промт 19 — Задача D-06 Суммаризация

> следующая задача 3. Суммаризация истории
> Реализуйте механизм сжатия контекста:
> Когда история становится слишком длинной:
> - отправьте её в LLM с запросом на краткое резюме
> - замените часть истории на это резюме
> Пример результата: Пользователь разрабатывает Telegram-бота на Python и хочет добавить платежи.
> После этого: продолжайте диалог, используя сокращённую версию
> если больше 5 (можно настроить) сообщений - хранить саммари

### Промт 20 — ok

> ok

_(Контекст: подтверждение дизайна D-06. Агент предложил: при превышении `HISTORY_SUMMARIZE_THRESHOLD` сообщений старые сообщения заменяются одним LLM-сгенерированным резюме, последние `HISTORY_KEEP_RECENT` остаются raw, при следующем триггере старое summary сливается с новыми — без цепочек. Fail-safe: при ошибке LLM возвращается исходная история, основной ответ не блокируется. См. [D-06 spec](../tasks/D-06_HISTORY_SUMMARIZATION.md))_

---

### Промт 21 — Merge всего + следующая задача

> смержи все в мастер и выполни следующую задачу

### Промт 22 — Разные уточнения

- «ты забыл про env.example» — добавил `UID`/`GID` в `.env.example`
- «в .env нет значений из .env.example» — добавил 8 новых env в `.env` (не трогая секретный token)
- «ты забыл про reset?» + подтверждение что reset только текущего юзера
- «- role: user / content: привет… разве планировался такой формат лога?» — объяснил что это нормальный YAML plain scalar
- «бот работает?» — проверка живого контейнера
- «проект подготовил к работе?» — статус сборки/деплоя

---

### Промт 23 — D-08 проверка

> D-08_CONTEXT_LOGGING.md посмотри эту задачу, она выполнена?

### Промт 24 — D-08 показать реализацию

> Перед отправкой запроса в модель: логируйте полный контекст / размер. покажи как это реализовано

### Промт 25 — Куда пишутся логи

> в какой файл это все пишется?

### Промт 26 — Задача D-09

> файл лога должен писать внутри проекта

### Промт 27 — Вариант A (dual output)

> Вариант а

_(Контекст: в D-09 агент предложил два варианта размещения файла логов:_
_A — dual output (stdout как раньше + ротируемый файл `data/logs/bot.log` внутри проекта через bind-mount, доступен без sudo) — выбран,_
_B — только файл (redirect stdout → file, ломает `docker compose logs`)._
_См. [D-09 spec](../tasks/D-09_LOG_FILE_ROTATION.md))_

---

### Промт 28 — Ack

> прием

_(Контекст: ack на отчёт агента о статусе — приняты утверждения про план, никаких возражений.)_

### Промт 29 — Доделать всё незавершённое

> сделай все что не сделано

---

## День 4 — 2026-04-17

### Промт 30 — Повтор D-08 + замечание про history

> 5. Логирование контекста (обязательно)
> [текст ТЗ повторно]
> покажи как это реализовано. В истории пользователя не видно не кол-во токенов, не то что приходит на вход в модель.

**Ответ агента**: объяснил разницу между history YAML (память) и log file (observability). Показал как `llm_request` event содержит весь контекст.

### Промт 31 — Toggle контекста

> контекст можно отключить/включить?

### Промт 32 — Обнаружил что «3 лимита в 0» не работает

> 2. не работет. проверь
> [лог диалога показывающий что бот помнит даже при max_messages=0, max_chars=0, threshold=0]

**Root cause**: прошлый совет «0 = disabled» был неверный. `0` в лимитах = «лимит не применяется» = безлимитно. Нужен отдельный флаг.

### Промт 33 — Вариант A (D-10 новый флаг)

> A

_(Контекст: в D-10 агент предложил два варианта «как выключить контекст целиком»:_
_A — ввести отдельный явный env-флаг `HISTORY_ENABLED` (default `true`), gate-ить `HistoryStore.get/append/replace` — выбран,_
_B — перегрузить семантику существующих лимитов (`HISTORY_MAX_MESSAGES=0` → stateless вместо «без лимита») — отвергнуто как двусмысленное._
_Задача всплыла из-за обнаружения prompt 32, что «3 лимита в 0» давали **безлимитную** память, а не stateless. См. [D-10 spec](../tasks/D-10_HISTORY_ENABLED_FLAG.md))_

**Результат**: D-10 · `HISTORY_ENABLED` env flag.

---

### Промт 34 — Финальный deliverable

> 1. сохрани в гит в отдельную папку с примерами [screenshot attached]
> 2. Сохрани все промты которые использовались в этом спринте.
> 3. Спринт завершен, обнови нужные файлы по документации и тд и тп

**Результат**: 
- Скриншот в [dialogs/stateless-vs-context-2026-04-17.png](../dialogs/stateless-vs-context-2026-04-17.png)
- Этот файл (`prompts-sprint-1.md`)
- Sprint-close по workflow

---

## Итог по промтам

- Промтов пользователя: **34+ крупных** (множество коротких подтверждений сгруппированы)
- Стиль: минимально-директивный, автономная работа предпочтительна, «follow spec strictly»
- Ключевые коррекции (сохранены как feedback-memory):
  - change-request.md — зеркало спринта, не очищать на merge
  - `HISTORY_ENABLED` — нужен явный флаг, не полагаться на «лимиты=0»
- Язык: русский (коммуникация), английский (код и коммиты)

## Что получилось в итоге

9 закрытых задач:
- C-01 Ollama → Lemonade migration
- C-02 Makefile pull-models fix
- D-04 Dialog history YAML persistent
- D-05 Context char limit
- D-06 History summarization
- D-07 Configurable system prompt
- D-08 Context logging (full payload)
- D-09 Dual output logging (stdout + rotating file)
- D-10 `HISTORY_ENABLED` flag (stateless toggle)

29/29 unit tests green, ruff clean, GitHub pushed, бот живой в проде с скриншотом-доказательством работы.
